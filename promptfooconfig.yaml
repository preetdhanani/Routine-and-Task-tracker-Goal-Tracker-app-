# Promptfoo evaluation configuration for Goal Tracker specialized agents
prompts:
  - "{{message}}"

providers:
  - id: echo

tests:
  - description: "Dummy local test to satisfy CI pipeline without API key"
    vars:
      message: "Hello World"
    assert:
      - type: icontains
        value: "Hello"
